server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/auth_db
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  data:
   redis:
    host: redis
    port: 6379

jwt:
  secret: your_super_secret_key_here
  expiration: 3600000