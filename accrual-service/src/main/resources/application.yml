spring:
  application:
    name: accrual-service
  datasource:
    url: jdbc:postgresql://localhost:5432/accrualdb
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
  kafka:
    bootstrap-servers: localhost:9092
  data:
    redis:
      host: redis
      port: 6379

server:
  port: 8083
