server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/auth_db
    username: postgres
    password: postgres
  jpa:  
    show-sql: true   
  data:
   redis:
    host: redis
    port: 6379

jwt:
  secret: ${JWT_SECRET}
  expiration: 600000